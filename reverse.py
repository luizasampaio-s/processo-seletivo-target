#Variável que receberá uma string
entrada_string = str(input('Digite algo: '))

# Invertendo string utilizando um slice em que o stepe se move do ultimo ao primeiro número.
print(entrada_string[::-1])